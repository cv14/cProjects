/***********************	
	Saurav Paudyal
	CV14
	MUTD209

Write a loop equivalent to the for
loop above without using && or || .

*************************/
#include <stdio.h>

int main(void){
  
	printf("type cat 02_exercise.c\n"); 
/*
	 int i=0;
         while(i<lim-1) {
              if((c=getchar()) != '\n') {
                   if(c != EOF) {
                         s[i] = c;
                   }
              }
              i++;
         }
*/

	return 0;
}
